namespace ScoopBox.Enums
{
    public enum VGpuOptions
    {
        Enabled,
        Disabled
    }

    public enum ReadOnlyOptions
    {
        True,
        False
    }

    public enum NetworkingOptions
    {
        Disable,
        Default
    }

    public enum AudioInputOptions
    {
        Enable,
        Disable,
        Default
    }

    public enum VideoInputOptions
    {
        Enable,
        Disable,
        Default
    }

    public enum ProtectedClientOptions
    {
        Enable,
        Disable,
        Default
    }

    public enum PrinterRedirectionOptions
    {
        Enable,
        Disable,
        Default
    }

    public enum ClipboardRedirectionOptions
    {
        Disable,
        Default
    }
}
